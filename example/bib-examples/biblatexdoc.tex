\documentclass{umassthesis}

\usepackage[hidelinks]{hyperref}  % if using hyperref, it must be loaded prior to biblatex
\usepackage[backend=bibtex8,style=numeric,natbib=true,sorting=nyt,sortcites=true,
		maxbibnames=10,minbibnames=5,maxcitenames=2,firstinits=true,
		isbn=false,language=american]{biblatex}
% Tweak the options to get IEEE style
%\usepackage[backend=bibtex8,style=ieee,natbib=true,sorting=nyt,sortcites=true,
%		maxbibnames=10,minbibnames=5,firstinits=true,
%		isbn=false,language=american]{biblatex}
\addbibresource{sample-bibliography.bib}
%\input{biblatex-LDFheaders}

\begin{document}

%\frontmatter
\mainmatter
Hi, here's some sample text.

And more, in which we test natbib-style citations: according to \citeauthor{Abril07}, also known as \citet{Abril07} the answer is maybe \cite{Abril07}.

\backmatter 

\nocite{*}  % print all bib entries
\printbibliography

\end{document}
